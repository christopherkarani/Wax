{
  "id": "wax-memory",
  "name": "Wax Memory",
  "version": "0.1.21",
  "kind": "memory",
  "description": "Exclusive OpenClaw memory slot for the shared Wax MCP HTTP endpoint. Never spawns a second writer.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "endpoint": {
        "type": "string",
        "description": "HTTP MCP endpoint for the Wax broker."
      }
    }
  },
  "uiHints": {
    "endpoint": {
      "label": "Wax MCP HTTP Endpoint",
      "placeholder": "http://127.0.0.1:3000/mcp"
    }
  }
}
