interface:
  display_name: "Wax Framework"
  short_description: "On-device RAG memory in Swift"
  default_prompt: "Use $wax to design correct on-device memory flows with the public Memory facade. Treat MemoryOrchestrator, WaxSession, SearchRequest, MiniLMEmbedder, PhotoRAG, and VideoRAG APIs as package-internal unless you are editing Wax itself."
