interface:
  display_name: "Wax Framework (Swift)"
  short_description: "On-device RAG memory in Swift"
  default_prompt: "Use $wax to design correct on-device memory flows with the public Memory facade (save/search/flush). For MCP agent memory tools, use $wax-mcp instead."
