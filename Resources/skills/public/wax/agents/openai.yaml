interface:
  display_name: "Wax Framework (Swift)"
  short_description: "Swift Memory/PhotoMemory/VideoMemory APIs"
  default_prompt: "Use $wax only when writing Swift client code against public Memory, PhotoMemory, or VideoMemory. Do not load at session start or for MCP remember/recall. wax-mcp is install/doctor only."
