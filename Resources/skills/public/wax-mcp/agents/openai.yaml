interface:
  display_name: "Wax MCP Memory"
  short_description: "Use Wax MCP tools for durable agent memory"
  default_prompt: "Use $wax-mcp remember, recall, and stats. The server auto-opens a transport-scoped session. Do not invent a session_id. WAX_MCP_TOOLS=legacy restores session_open and session_close. Native Hermes uses memory.provider wax-memory and wax_* tools instead of this MCP skill; never plugins.enabled."
